# Copy to your environment (e.g. `export $(cat .env)` or a process manager).
# Everything is optional — the terminal runs with zero keys.

# HTTP port
#PORT=8432

# Optional: richer company news & profiles (free tier: 60 calls/min)
# Get a key at https://finnhub.io
#FINNHUB_API_KEY=
